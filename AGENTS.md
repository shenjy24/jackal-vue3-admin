# AGENTS.md

本文件是 `jackal-vue3-admin` 仓库的 AI 编码协作规范。修改代码时优先遵循这里的约定，并结合现有代码风格执行。

## 项目定位

- `jackal-java-template` 是完整的 Java 21 + Spring Boot 3 后端项目框架，已经包含后台认证、用户、角色、菜单和权限等基础能力。
- `jackal-vue3-admin` 是与其配套的 Vue 3 管理后台前端，负责承载后端 admin 模块的管理界面。
- 当前建设目标是完善可复用的管理后台框架，优先打通登录、会话恢复、动态菜单、动态路由、角色权限和按钮权限等完整 RBAC 链路。
- 当前只关注 `jackal-java-template` 的 admin 侧接口和业务能力，`controller/web` 等用户侧功能暂不开发、不对接，也不要为了用户侧场景增加兼容代码。
- 框架完成后，新增业务功能应尽量只需在后端添加菜单及权限配置，并在前端开发、注册对应页面，不应重复改造登录、布局、路由和权限基础设施。

## 技术栈与目录

- 使用 Vue 3、Vite、TypeScript、Pinia、Vue Router、Element Plus、Vue I18n 和 Axios。
- `src/api`：admin 接口定义和统一请求封装。
- `src/views`：管理后台页面，按业务模块组织。
- `src/router`：静态路由、动态菜单路由、组件和图标映射。
- `src/stores`：登录态、权限、标签页和全局设置等状态。
- `src/layouts`：管理后台整体布局。
- `src/components`、`src/hooks`、`src/directives`：可复用组件、组合式逻辑和权限指令。
- `src/types`：接口、菜单、权限和通用业务类型。
- `src/i18n`：菜单和页面国际化文案。

## 后端协作边界

- 后端实现以同级仓库 `../jackal-java-template` 为准，开始开发前应阅读相关 admin Controller、Qo、Vo、Service 和权限实现，不根据接口名称猜测字段。
- 只对接 `com.tech.controller.admin` 及其关联的认证、权限和管理功能；除非任务明确要求，不处理 web 用户侧接口。
- 前端 API 路径需结合 `VITE_API_BASE_URL`、开发代理和部署代理理解，避免重复或遗漏 `/admin` 前缀。
- 保持后端统一响应、错误码和认证机制，不在页面中自行兼容另一套响应结构。
- 后端接口、字段或权限模型尚未具备时，应明确指出依赖，不用前端硬编码虚假数据掩盖缺失能力。

## 权限框架约定

- 登录、会话恢复和退出统一由 `src/api/auth.ts` 与 `src/stores/auth.ts` 管理。
- 用户、角色、菜单和权限数据以后端 admin 会话及权限接口为事实来源。
- 菜单由后端返回，前端通过 `src/router/dynamic.ts` 转换为动态路由，并由后台布局统一渲染。
- 后端菜单中的 `component` 使用相对 `src/views` 的无扩展名路径，并且只能在 `import.meta.glob("/src/views/**/*.vue")` 生成的构建期模块集合中解析；禁止加载集合外路径。
- 页面访问权限由动态菜单和路由守卫控制，按钮级权限统一复用 `v-permission` 或已有权限工具。
- 权限码由后端定义并保持稳定，前端只消费权限码；不要通过角色名称、用户名或页面路径推断权限。
- 用户、角色、菜单管理是权限模块的核心页面。修改其中任一模块时，应检查角色与菜单、角色与用户、菜单与权限码之间的联动。
- admin 与 web 的认证入口、Token/Cookie 和权限语义不得混用。

## 新增业务功能

新增管理功能通常按以下顺序完成：

1. 确认 `jackal-java-template` admin 模块已有对应接口、菜单和权限码。
2. 在 `src/api` 中增加类型明确的接口封装，页面不得直接调用 Axios。
3. 在 `src/views/<module>` 中开发页面，并优先复用现有 CRUD 组件和 `useCrud`。
4. 在后端添加或调整菜单，使菜单的 `component` 与页面相对 `src/views` 的无扩展名路径一致；需要新图标时注册图标 key。
5. 配置菜单 `code`、`path` 和所需权限码，保持权限标识、路由地址和组件路径职责独立。
6. 在 `src/i18n/locales` 中补充菜单及页面文案。
7. 为新增、修改、删除等操作配置按钮权限，并验证无权限用户无法看到或执行相关操作。

新增功能不应要求手工维护另一份前端侧菜单树。除登录页、错误页等基础页面外，业务导航以后端菜单为准。

## 代码风格

- 优先保持现有 Composition API、TypeScript、Pinia 和 Element Plus 使用方式。
- 新代码应提供明确类型，避免无必要的 `any`、非空断言和类型强制转换。
- 页面负责展示、交互和参数组织；请求逻辑放在 `api`，跨页面状态放在 `stores`，可复用业务流程放在 `hooks`。
- Entity/Qo/Vo 等后端模型不要原样照搬为一个万能前端类型，应按请求和响应职责定义类型。
- 复用已有布局、CRUD、错误处理、权限判断和国际化机制，不另建平行实现。
- 不做与当前任务无关的重构、全局格式化、依赖升级或视觉改版。
- 新增依赖前先确认现有依赖无法合理完成任务，并说明用途和影响。
- 只在逻辑不容易一眼看懂时添加简短注释。

## 接口与错误处理

- 所有请求统一经过 `src/api/client.ts`，保持 Cookie、成功码、错误提示和未登录跳转行为一致。
- 接口方法遵循后端 admin Controller 的实际请求方式和参数结构，不自行改变字段名或分页语义。
- 列表、分页、新增、修改和删除优先沿用现有 `src/api/crud.ts`、`useCrud` 及通用组件模式。
- 页面需要处理加载态、空数据、提交中状态和失败反馈，避免重复提交。
- 不在日志、提示或本地持久化中暴露密码、Token、Cookie 等敏感信息。
- 权限校验不能只依赖前端隐藏按钮，真正的授权仍由后端 admin 权限机制保证。

## 修改约束

- 修改前先阅读相关前端页面、API、Store、路由，以及后端对应 admin 实现。
- 工作区可能存在用户未提交改动，不回滚、不覆盖与当前任务无关的修改。
- 不随意改变已有路由、请求参数、响应字段、权限码或组件路径，除非任务明确要求并已同步检查后端影响。
- 修改权限基础设施时，要验证刷新恢复、退出登录、无权限访问、菜单层级、动态路由重复注册和 403/404 行为。
- 修改部署配置、环境变量、Docker 或 Nginx 时，需要说明开发和生产环境影响，且不得提交真实密钥或生产敏感配置。

## 验证

代码改动后优先运行：

```bash
npm run build
```

涉及格式或静态检查问题时运行：

```bash
npm run lint
```

涉及登录、权限或动态路由时，还应至少手工验证：

- 登录后可以恢复 admin 会话并进入后台布局。
- 后端菜单可以正确生成侧边栏和动态路由，刷新后仍可访问。
- 不同角色只能看到被授权的菜单和按钮。
- 未登录、无权限和未知路由分别进入预期页面或提示。
- 新增菜单的 `component` 能匹配 `src/views` 下的页面文件，即可打开对应业务页面。

如果无法执行验证命令，需要在最终回复中说明原因。

## 协作要求

- 优先完成可复用的 admin 基础能力和权限闭环，再扩展普通业务页面。
- 发现前后端契约不一致时，应同时说明前端现状、后端实际定义和建议采用的统一方案。
- 最终回复简洁说明改了什么、验证了什么，以及仍需注意的后端 admin 依赖。
