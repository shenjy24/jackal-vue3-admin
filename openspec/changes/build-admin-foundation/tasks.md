## 1. 项目基础

- [x] 1.1 增加 Vue Router、Pinia、Element Plus、Axios、vue-i18n 以及相关类型依赖。
- [x] 1.2 建立基础 `src` 目录结构，包括 app、api、router、stores、layouts、components、hooks、directives、i18n、styles、types、utils 和 views。
- [x] 1.3 配置应用启动流程，安装 router、Pinia、Element Plus、i18n 和全局样式。
- [x] 1.4 增加标准 API 响应、用户、角色、权限、菜单、路由元数据和分页的共享 TypeScript 类型。

## 2. API 客户端和认证会话

- [x] 2.1 实现统一 Axios 客户端，服务请求统一使用 POST，并携带 HttpOnly Cookie 会话所需的 credentials。
- [x] 2.2 实现标准 `code`、`message`、`data` 响应结构的解包和标准化。
- [x] 2.3 实现未登录和无权限响应处理，在需要时清理前端会话状态。
- [x] 2.4 增加 `POST /auth/login`、`POST /auth/session` 和 `POST /auth/logout` 认证 API 模块。
- [x] 2.5 实现 auth/session Pinia store，管理用户信息、角色、权限、菜单、会话就绪状态、登录、会话恢复和退出登录。

## 3. 动态路由和 RBAC

- [x] 3.1 定义登录页和错误页等静态公共路由。
- [x] 3.2 定义认证后管理后台页面使用的受保护 shell 路由结构。
- [x] 3.3 实现后端菜单到 Vue Router 路由的转换。
- [x] 3.4 实现后端菜单组件 key 到前端组件的白名单映射。
- [x] 3.5 实现后端菜单图标 key 到前端图标的白名单映射。
- [x] 3.6 实现路由守卫，负责恢复会话状态、注册动态路由并阻止未授权访问。
- [x] 3.7 实现用于权限码检查的 `hasPermission` 工具函数或 composable。
- [x] 3.8 实现用于按钮级权限控制的 `v-permission` 指令。

## 4. 后台 Shell 布局

- [x] 4.1 实现认证后的后台布局，包括侧边栏、顶部栏、面包屑和内容出口。
- [x] 4.2 根据授权菜单树渲染侧边栏导航。
- [x] 4.3 实现已访问后台页面的多标签页状态管理。
- [x] 4.4 实现标签页切换和关闭交互。
- [x] 4.5 实现由路由 `meta.keepAlive` 控制的 keep-alive 集成。
- [x] 4.6 增加 403 和 404 页面，并接入路由行为。

## 5. 国际化

- [x] 5.1 增加 vue-i18n 配置和中英文 locale 资源。
- [x] 5.2 增加导航、按钮、表单、消息和 CRUD 操作的通用翻译 key。
- [x] 5.3 通过路由元数据中的 i18n key 渲染菜单、面包屑、标签页和路由标题。
- [x] 5.4 增加语言切换状态和持久化。

## 6. CRUD 模式

- [x] 6.1 增加可复用 CRUD 请求类型和分页类型。
- [x] 6.2 实现用于表格筛选的查询表单模式或组件。
- [x] 6.3 实现通过统一 API 客户端加载分页数据的数据表格模式。
- [x] 6.4 实现创建和更新流程使用的表单弹窗模式。
- [x] 6.5 增加使用这些模式的用户管理 CRUD 示例页面。
- [x] 6.6 增加使用这些模式的角色管理 CRUD 示例页面。
- [x] 6.7 增加使用这些模式的菜单管理 CRUD 示例页面。

## 7. 部署

- [x] 7.1 增加用于服务构建产物和 history 模式路由 fallback 的 Nginx 配置。
- [x] 7.2 增加使用 Node 构建阶段和 Nginx 运行阶段的 Dockerfile。
- [x] 7.3 增加 Nginx 和 Docker 使用方式的部署说明。
- [x] 7.4 确保环境变量支持本地和部署构建的 API base URL 配置。

## 8. 验证

- [x] 8.1 验证登录后会进入会话加载并初始化后台 shell。
- [x] 8.2 验证页面刷新后可以恢复会话、菜单、权限、动态路由、标签页和 keep-alive 状态。
- [x] 8.3 验证未授权用户无法访问隐藏路由或按钮。
- [x] 8.4 验证菜单和通用操作可以渲染中文与英文标签。
- [x] 8.5 成功运行生产构建。
